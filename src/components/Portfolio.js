import React from 'react';

// Import components
import Project from "./Project";

const Portfolio = () => {
    return <section id='portfolio' className='section'>Portfolio</section>;
};

export default Portfolio;